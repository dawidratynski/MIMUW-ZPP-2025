// To run the project, rename this file to env.js and fill the required variables

// Note that the Google Maps Api Key is a client-side api key and must be exposed
// to the user for the map to work

// Make sure that AUTH0 Domain and Audience match backend settings (.env file in root folder)

export const GOOGLE_MAPS_API_KEY = '';
export const AUTH0_DOMAIN = '';
export const AUTH0_CLIENT_ID = '';
export const AUTH0_AUDIENCE = '';